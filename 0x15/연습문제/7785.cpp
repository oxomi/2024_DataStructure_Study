//이분탐색과 투포인터로도 풀 수 있다고 함.
#include <bits/stdc++.h>
using namespace std;

unordered_set<string> s;

int main(){
    int num;
    cin >> num;

    while(num--){
        string name, log;
        cin >> name >> log;
        
    }
}